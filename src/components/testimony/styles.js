import { COLOR } from '../../constants';

export const testimonyContainer = {
  padding: '20px',
  marginTop: '20px',
  marginBottom: '10px',
  background: COLOR.SLIGHT_ACCENT_BG,
};

export const testimonyCardContainer = {
  display: 'flex',
  justifyContent: 'space-evenly',
};

export const testimonyCard = {
  display: 'flex',
  flexDirection: 'column',
  width: '150px',
  padding: '20px',
  background: 'var(--white)',
  borderRadius: '20px',
  boxShadow: 'var(--box-shadow-raised)',
};
